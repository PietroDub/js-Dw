// Anonymous funcion
(function (a, b, c){
    return a + b + c
})
//Function expression, atribuir a uma variável o valor de uma função

var sum = function (a, b) {
    return a + b
}
const resulta = sum(6,90)
console.log(resulta)
